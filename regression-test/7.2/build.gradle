dependencies {
    testImplementation project(':baseline')

    testImplementation 'io.confluent:kafka-schema-registry:7.2.4'
    testImplementation 'io.confluent:kafka-avro-serializer:7.2.4'

    // Required to ensure the correct Kafka Server version
    testImplementation('com.salesforce.kafka.test:kafka-junit5') {
        version {
            strictly '3.2.5'
        }
    }
}
